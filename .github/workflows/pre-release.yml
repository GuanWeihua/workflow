name: Pre-release

on:
    push:
        branches: 
            - release/**
jobs:
    prerelease:

        runs-on: windows-latest

        strategy:
            matrix:
                node-version: [14.x]

      steps:
            - name: Checkout
              uses: actions/checkout@v2
              with:
                    ref: ${{ github.ref }}

            - run: echo ${{ github.ref }}